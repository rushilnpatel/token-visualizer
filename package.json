{
  "name": "token-visualizer",
  "version": "1.0.0-next",
  "description": "Representation of token structure",
  "main": "./dist/index",
  "types": "./dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -f dist",
    "watch:build": "tsc --watch",
    "watch:server": "nodemon './dist/index.js' --watch './dist'",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "repository": {
    "type": "git",
    "url": "clear"
  },
  "author": "Rushil",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.61.0",
    "typescript": "^5.0.2",
    "vite": "^4.2.1"
  }
}
